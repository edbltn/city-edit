# Python
server/env/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Environment
.env
*.env
!.env.example

# Editor
*.swp
*.swo
*~
.DS_Store

# Node
node_modules/
npm-debug.log
yarn-error.log

# Bun
bun.lockb

# Logs
*.log

# Terraform
terraform/.terraform/
terraform/terraform.tfstate*
terraform/terraform.tfvars

# Google Cloud SDK
google-cloud-sdk/
*.tar.gz

# Redis
dump.rdb

# Cache
server/cache/

# Citibike data downloads
server/citibike_data/

# Raw Lyft ride imports (one-off import source, ~191M — not committed)
server/lyft_data/

# Terraform plan output (binary, regenerated)
terraform/tfplan

# Screenshots and images (local only, not client assets)
Screenshot*.png
*.jpg

# Generated map previews (captured by screenshots/capture.py)
client-react/public/previews/*.png

# Generated/downloaded OSM graph data (rebuilt in the image via refresh_osm.py)
server/osm_data/

# Database dumps
*.dump

# Local scratch / debug artifacts
/debug
/af2641f3-*.png

# Locust run output and venv
loadtest/env/
loadtest/sf_run*
loadtest/*.log

# streetscape_blocks generated data (regenerable, too big for git)
server/streetscape_blocks/output/

# osmnx http cache + eval fixtures (regenerable, too big for git)
server/streetscape_blocks/cache/
server/streetscape_blocks/eval/*.npz
