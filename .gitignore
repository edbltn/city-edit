# Python
server/env/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Environment
.env
*.env
!.env.example

# Editor
*.swp
*.swo
*~
.DS_Store

# Node
node_modules/
npm-debug.log
yarn-error.log

# Bun
bun.lockb

# Logs
*.log

# Terraform
terraform/.terraform/
terraform/terraform.tfstate*
terraform/terraform.tfvars

# Google Cloud SDK
google-cloud-sdk/
*.tar.gz

# Redis
dump.rdb

# Cache
server/cache/

# Citibike data downloads
server/citibike_data/

# Screenshots and images (local only, not client assets)
Screenshot*.png
*.jpg

# Generated map previews (captured by screenshots/capture.py)
client-react/public/previews/*.png

# Generated/downloaded OSM graph data (rebuilt in the image via refresh_osm.py)
# OSM data artifacts are not tracked — EXCEPT the stable edge-id registries,
# which must persist across graph rebuilds/deploys (see server/edge_registry.py).
server/osm_data/**
!server/osm_data/*/
!server/osm_data/*/edge_registry.json.gz

# Database dumps
*.dump

# Local scratch / debug artifacts
/debug
/af2641f3-*.png

# Locust run output and venv
loadtest/env/
loadtest/sf_run*
loadtest/*.log
