# Generated print files, manifests and seed SQL — the build is deterministic,
# so these are always exactly reproducible from this directory.
out/
# Diffusion output. Hours of GPU time and tens of MB per sheet, keyed by
# payload+prompt, and regenerable — but NOT cheap, which is why it lives
# outside out/ where --clean cannot reach it.
art-cache/
# Local venv symlink.
env
__pycache__/
